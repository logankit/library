package com.equifax.c2o.usevalidateffid;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main application class for the useValidateFFID project
 * that demonstrates using the FFID validation library
 */
@SpringBootApplication
public class UseValidateFfidApplication {
    public static void main(String[] args) {
        SpringApplication.run(UseValidateFfidApplication.class, args);
    }
}
